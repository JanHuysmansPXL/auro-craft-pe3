{# DIT IS EEN TEMPLATE -- HEAD EN BODY STEKEN IN DE INDEX-TWIG #}
{% extends "_index" %}

{% block content %}

{% include "_snippets/_navbar" %}
{# DIT IS HET BEGIN VAN DE BLOCK #}

{% set image = entry.contactImage.one() %}


<section class="container-fluid p-md mt-xl" style="background-image: url('{{image.url}}')">   
    <h1 class="txt-heading_xxl">{{entry.sectionTitle}}</h1>
    <p class="txt-heading_lg">{{entry.sectionBodytext}}</p>
</section>

<div class="container-fluid login-frame">

  <form class="login-form txt-light txt-body_md" method="post" action="">
    {{ craft.freeform.form("contactForm", {
      labelClass: "login-label",
      inputClass: "form-input-label",
      instructionsBelowField: "true",
    }).render() }}
  </form>
</div>

{% endblock %}
